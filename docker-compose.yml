services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: was
      POSTGRES_USER: was
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-was_dev_password}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U was"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3004:3000"
    environment:
      - DATABASE_URL=postgresql://was:${POSTGRES_PASSWORD:-was_dev_password}@postgres:5432/was
      - NODE_ENV=production
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  worker-oref-current:
    build:
      context: .
      dockerfile: Dockerfile.worker
    command: ["node", "dist/workers/oref-current.js"]
    environment:
      - DATABASE_URL=postgresql://was:${POSTGRES_PASSWORD:-was_dev_password}@postgres:5432/was
      - OREF_BASE_URL=${OREF_BASE_URL:-https://www.oref.org.il}
      - SSH_PROXY_HOST=${SSH_PROXY_HOST:-curl@192.168.5.249}
    volumes:
      - ~/.ssh/id_rsa:/root/.ssh/id_rsa:ro
      - ~/.ssh/known_hosts:/root/.ssh/known_hosts:ro
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  worker-oref-history:
    build:
      context: .
      dockerfile: Dockerfile.worker
    command: ["node", "dist/workers/oref-history.js"]
    environment:
      - DATABASE_URL=postgresql://was:${POSTGRES_PASSWORD:-was_dev_password}@postgres:5432/was
      - OREF_BASE_URL=${OREF_BASE_URL:-https://www.oref.org.il}
      - SSH_PROXY_HOST=${SSH_PROXY_HOST:-curl@192.168.5.249}
    volumes:
      - ~/.ssh/id_rsa:/root/.ssh/id_rsa:ro
      - ~/.ssh/known_hosts:/root/.ssh/known_hosts:ro
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  ai-cron:
    build:
      context: .
      dockerfile: Dockerfile.worker
    command: ["node", "dist/workers/ai-assessment.js"]
    environment:
      - DATABASE_URL=postgresql://was:${POSTGRES_PASSWORD:-was_dev_password}@postgres:5432/was
      - AWS_REGION=${AWS_REGION:-us-east-1}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - BEDROCK_MODEL=${BEDROCK_MODEL:-us.anthropic.claude-opus-4-6-v1}
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  worker-rss-feeds:
    build:
      context: .
      dockerfile: Dockerfile.worker
    command: ["node", "dist/workers/rss-feeds.js"]
    environment:
      - DATABASE_URL=postgresql://was:${POSTGRES_PASSWORD:-was_dev_password}@postgres:5432/was
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  worker-telegram-scraper:
    build:
      context: .
      dockerfile: Dockerfile.worker
    command: ["node", "dist/workers/telegram-scraper.js"]
    environment:
      - DATABASE_URL=postgresql://was:${POSTGRES_PASSWORD:-was_dev_password}@postgres:5432/was
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  worker-aviation:
    build:
      context: .
      dockerfile: Dockerfile.worker
    command: ["node", "dist/workers/aviation-tracker.js"]
    environment:
      - DATABASE_URL=postgresql://was:${POSTGRES_PASSWORD:-was_dev_password}@postgres:5432/was
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  worker-gdelt:
    build:
      context: .
      dockerfile: Dockerfile.worker
    command: ["node", "dist/workers/gdelt-collector.js"]
    environment:
      - DATABASE_URL=postgresql://was:${POSTGRES_PASSWORD:-was_dev_password}@postgres:5432/was
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  worker-web-scrapers:
    build:
      context: .
      dockerfile: Dockerfile.worker
    command: ["node", "dist/workers/web-scrapers.js"]
    environment:
      - DATABASE_URL=postgresql://was:${POSTGRES_PASSWORD:-was_dev_password}@postgres:5432/was
      - SSH_PROXY_HOST=${SSH_PROXY_HOST:-curl@192.168.5.249}
    volumes:
      - ~/.ssh/id_rsa:/root/.ssh/id_rsa:ro
      - ~/.ssh/known_hosts:/root/.ssh/known_hosts:ro
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  worker-ship-tracker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    command: ["node", "dist/workers/ship-tracker.js"]
    environment:
      - DATABASE_URL=postgresql://was:${POSTGRES_PASSWORD:-was_dev_password}@postgres:5432/was
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  worker-corroboration:
    build:
      context: .
      dockerfile: Dockerfile.worker
    command: ["node", "dist/workers/corroboration.js"]
    environment:
      - DATABASE_URL=postgresql://was:${POSTGRES_PASSWORD:-was_dev_password}@postgres:5432/was
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  llm-geocoder:
    build:
      context: .
      dockerfile: Dockerfile.worker
    command: ["node", "dist/workers/llm-geocoder.js"]
    environment:
      - DATABASE_URL=postgresql://was:${POSTGRES_PASSWORD:-was_dev_password}@postgres:5432/was
      - AWS_REGION=${AWS_REGION:-us-east-1}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - BEDROCK_MODEL=${BEDROCK_MODEL:-us.anthropic.claude-opus-4-6-v1}
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data:
